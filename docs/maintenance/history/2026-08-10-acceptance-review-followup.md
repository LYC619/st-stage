# 2026-08-10 CC 复审收尾

## 结论

CC 复审确认上一轮发布前阻断项已修复，可以进入真实 SillyTavern 验收。本次接受两个异步旧快照问题并修复；对“预设与本地副本可同时启用并造成静默歧义”的描述不予采纳，因为启用入口最终调用 `bindPack`，同角色、服装和图名会触发地址冲突并拒绝提交。该产品路径已增加专门回归测试。

## 已修复

1. 普通图包保存本地时，每张图片完成下载、压缩和写入后重新读取 settings，确认目标包、目标立绘及原始 URL 仍未变化，再以最新图包和最新 settings 提交单张本地化结果。等待期间的替换、重命名或其他设置变化不再被旧快照覆盖。
2. 预设包本地化仍不改写远程预设；各图片完成后记录本地结果，创建“（本地）”副本前再次读取并核对最新预设，只覆盖本地 URL 字段，保留等待期间更新的最新元数据。
3. 物理文件全部删除成功后，以提交点的最新 settings 调用 `removePacks`，避免删除请求等待期间新增或修改的无关图包被旧快照覆盖。
4. 新增预设与其本地副本同地址的绑定回归测试，证明第二次启用返回冲突且原绑定保持不变；因此没有添加与真实行为相反的用户警告或延期事项。

## TDD 证据

- 新增本地化并发测试在修复前观察到旧结果覆盖新 URL，修复后保留并发替换并报告该张失败。
- 新增删除并发测试在修复前观察到并发新增图包丢失，修复后只移除目标包。
- 预设/本地副本冲突测试在现有实现上直接通过，确认冲突保护已经存在。

## Fresh Verification

- Vitest：54 files / 634 passed。
- TypeScript `tsc --noEmit`：通过。
- ESLint：通过。
- 扩展构建测试：15/15 通过。
- Next.js 16.2.6 webpack production build：通过。
- Mobile Playwright：Pixel 7 与 Galaxy S8 共 22/22 通过。
- 固定构建时间 `2026-08-10 19:52` 生成 `0.9.0+202608101952`；根目录与 `st-distribution/` 的 `index.js`、`bundle.js`、`style.css`、`version.json` SHA-256 一致。
- `st-distribution/` 共 6 个文件，图片、`public/`、`reference/`、预设源命中数为 0。
- 工作区 `git diff --check` 通过；代码提交为 `10e5628`。

## Boundary

分支为 `codex/acceptance-round2`；未合并、未推送。全部真实 SillyTavern 验收项仍保持未完成，尤其是缓存更新、真实模型输出、文件 API、本地化和四个常用 API 渠道。产品语义版本仍为 `0.9.0`，真实验收通过后再决定并执行 `1.0.0`。
