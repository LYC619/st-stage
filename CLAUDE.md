# Claude Code Project Guidance

Use `AGENTS.md` as the shared cross-agent entry point. Before implementation, read:

- `docs/maintenance/CURRENT.md`
- `docs/maintenance/PROTOCOL.md`
- `docs/maintenance/DEFERRED.md` when relevant

Do not treat Claude Code's private project memory as the authoritative project status. Reconcile any mismatch against Git and the repository maintenance history, then update the shared documents at handoff boundaries.
